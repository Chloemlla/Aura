{
  "schemaVersion": 1,
  "policyKind": "npmOverridePolicy",
  "manifest": "functions/package.json",
  "lockfile": "functions/package-lock.json",
  "rationale": "Every entry in the functions overrides block exists to hold a transitive dependency at or above a published advisory's fixed version. A pinned override is a ceiling as well as a floor, so a pin added for security can itself become the vulnerable version. This policy records the minimum safe version and the advisory that set it, and the gate fails when an override drops below its floor or when the lockfile resolves something older.",
  "reviewedOn": "2026-09-12",
  "overrides": [
    {
      "package": "protobufjs",
      "minimumSafeVersion": "7.6.5",
      "advisories": ["GHSA-j3f2-48v5-ccww"],
      "reason": "Denial of service via infinite loop in .proto option parsing. Affects 7.5.0 through 7.6.4; fixed in 7.6.5. The override previously pinned 7.6.4, which the advisory later covered."
    },
    {
      "package": "qs",
      "minimumSafeVersion": "6.16.0",
      "advisories": ["GHSA-4mjr-xmp4-gh2g", "GHSA-x5fp-wj9c-mxmx"],
      "reason": "Denial of service and array-limit bypasses in attacker-controlled query values. Both advisories are fixed in 6.16.0. Reached transitively through firebase-functions."
    },
    {
      "package": "form-data",
      "minimumSafeVersion": "2.5.6",
      "advisories": ["CVE-2026-12143"],
      "reason": "Unsafe random boundary generation. Fixed in 2.5.6 on the 2.x line."
    },
    {
      "package": "uuid",
      "minimumSafeVersion": "11.1.1",
      "advisories": ["CVE-2026-41907", "CVE-2026-41988"],
      "reason": "Fixed in 11.1.1 on the 11.x line."
    }
  ],
  "sourceUrls": [
    "https://api.osv.dev/v1/query",
    "https://github.com/advisories/GHSA-j3f2-48v5-ccww",
    "https://github.com/advisories/GHSA-4mjr-xmp4-gh2g",
    "https://github.com/advisories/GHSA-x5fp-wj9c-mxmx"
  ]
}
