{
  "lastReviewed": "2026-09-14",
  "limitsContract": "app/src/main/java/com/chloemlla/aura/service/LibraryTransferContract.kt",
  "documentedLimitKeys": [
    "MAX_FAVORITES",
    "MAX_COLLECTIONS",
    "MAX_COLLECTION_ITEMS",
    "MAX_SEARCH_HISTORY_ITEMS",
    "MAX_ROTATION_EXCLUSIONS",
    "MAX_LOCAL_WALLPAPERS",
    "MAX_WALLPAPER_HISTORY_ITEMS",
    "MAX_FAVORITES_DOCUMENT_CHARS",
    "MAX_COLLECTION_DOCUMENT_BYTES",
    "MAX_LIBRARY_DOCUMENT_CHARS",
    "MAX_TEXT_CHARS",
    "MAX_URL_CHARS",
    "MAX_COLLECTION_NAME_CHARS",
    "MIN_SHARE_TOKEN_CHARS",
    "MAX_SHARE_TOKEN_CHARS",
    "MAX_QR_IMAGE_BYTES",
    "MAX_QR_IMAGE_DIMENSION",
    "MAX_QR_IMAGE_PIXELS"
  ],
  "formats": {
    "favorites": {
      "currentVersion": 2,
      "sourceFile": "app/src/main/java/com/chloemlla/aura/service/FavoritesExporter.kt",
      "rootClass": "FavoritesExportFile",
      "itemClass": "FavoriteExportItem",
      "mimeType": "application/json",
      "fileExtension": ".json",
      "limitKeys": [
        "MAX_FAVORITES",
        "MAX_FAVORITES_DOCUMENT_CHARS",
        "MAX_TEXT_CHARS",
        "MAX_URL_CHARS"
      ],
      "fields": {
        "version": { "type": "Int", "required": true, "since": 1, "description": "Schema version. Versions 1 and 2 are accepted; version 2 is current." },
        "exportedAt": { "type": "Long", "required": true, "since": 1, "description": "Unix epoch millis when the export was created." },
        "items": { "type": "List<FavoriteExportItem>", "required": true, "since": 1, "description": "Exported favorite entries." }
      },
      "itemFields": {
        "id": { "type": "String", "required": true, "since": 1, "description": "Provider item ID. Sanitized on import." },
        "source": { "type": "String", "required": true, "since": 1, "description": "ContentSource enum name. Validated against known sources on import." },
        "type": { "type": "String", "required": true, "since": 1, "description": "WALLPAPER or SOUND." },
        "thumbnailUrl": { "type": "String", "required": true, "since": 1, "description": "HTTPS thumbnail URL. May be blank for SOUND or locator-free local media." },
        "fullUrl": { "type": "String", "required": true, "since": 1, "description": "HTTPS full-resolution URL. Blank only for locator-free local media." },
        "name": { "type": "String", "default": "", "since": 1, "description": "Display name." },
        "width": { "type": "Int", "default": 0, "since": 1, "description": "Image width in pixels." },
        "height": { "type": "Int", "default": 0, "since": 1, "description": "Image height in pixels." },
        "duration": { "type": "Double", "default": 0.0, "since": 1, "description": "Audio duration in seconds." },
        "tags": { "type": "String?", "since": 1, "description": "Comma-separated tags." },
        "colors": { "type": "String?", "since": 1, "description": "Comma-separated hex colors." },
        "category": { "type": "String?", "since": 1, "description": "Provider-assigned category." },
        "uploaderName": { "type": "String?", "since": 1, "description": "Original uploader/creator name." },
        "sourcePageUrl": { "type": "String?", "since": 1, "description": "HTTPS link to the original source page." },
        "license": { "type": "String?", "since": 1, "description": "License identifier or display label." },
        "fileSize": { "type": "Long?", "since": 1, "description": "File size in bytes." },
        "fileType": { "type": "String?", "since": 1, "description": "MIME type or file extension." },
        "views": { "type": "Long?", "since": 1, "description": "View count from provider." },
        "favoritesCount": { "type": "Long?", "since": 1, "description": "Provider favorite/like count." },
        "addedAt": { "type": "Long?", "since": 1, "description": "Unix epoch millis when the user favorited this item." },
        "sourceAvailability": { "type": "String?", "since": 1, "description": "AVAILABLE or SOURCE_UNAVAILABLE. Normalized to AVAILABLE on null/blank." },
        "sourceAvailabilityReason": { "type": "String?", "since": 1, "description": "Human-readable reason when source is unavailable." },
        "localMedia": { "type": "Boolean", "default": false, "since": 2, "description": "True when the media bytes stay on the exporting device and the restored row must offer Relink." },
        "localMediaSha256": { "type": "String", "default": "", "since": 2, "description": "Optional SHA-256 used to verify a replacement without exposing its original path." }
      },
      "importValidation": [
        "id must be non-blank after trim",
        "source must match a known ContentSource enum value",
        "type must be WALLPAPER or SOUND",
        "thumbnailUrl must be HTTPS (may be blank for SOUND type)",
        "fullUrl must be non-blank HTTPS unless localMedia is true",
        "Version 2 localMedia rows use a portable ID, blank locators, and an optional SHA-256 digest",
        "sourcePageUrl, if present, must be HTTPS",
        "sourceAvailability is normalized to AVAILABLE if null or blank",
        "An item count or document size above the shared contract limit refuses the whole import before any database write",
        "Duplicate identities already in the file or target library are counted as skipped; invalid entries are counted as failed"
      ],
      "versionPolicy": {
        "bumpRule": "Increment version when adding required fields or changing field semantics. Optional nullable fields may be added without a version bump.",
        "forwardCompat": "Unknown fields are silently ignored by Moshi. Future versions with higher version numbers are rejected with user-visible copy.",
        "backwardCompat": "Version 1 files remain importable. Version 2 adds locator-free local media metadata.",
        "unsupportedVersionCopy": "This file was exported by a newer version of Aura. Please update the app to import it."
      },
      "privacyExclusions": [
        "Export does not include provider API keys or user credentials",
        "Export does not include download file paths or device storage locations",
        "Local favorite locators are replaced with portable IDs and blank URLs",
        "Export does not include community identity UIDs or Firebase auth state",
        "sourcePageUrl may contain third-party provider URLs with query parameters"
      ]
    },
    "collections": {
      "currentVersion": 1,
      "sourceFile": "app/src/main/java/com/chloemlla/aura/service/CollectionExporter.kt",
      "rootClass": "CollectionExportFile",
      "itemClass": "CollectionExportItem",
      "mimeType": "application/json",
      "fileExtension": ".json",
      "limitKeys": [
        "MAX_COLLECTION_ITEMS",
        "MAX_COLLECTION_DOCUMENT_BYTES",
        "MAX_TEXT_CHARS",
        "MAX_URL_CHARS",
        "MAX_COLLECTION_NAME_CHARS",
        "MIN_SHARE_TOKEN_CHARS",
        "MAX_SHARE_TOKEN_CHARS",
        "MAX_QR_IMAGE_BYTES",
        "MAX_QR_IMAGE_DIMENSION",
        "MAX_QR_IMAGE_PIXELS"
      ],
      "fields": {
        "version": { "type": "Int", "required": true, "since": 1, "description": "Schema version. Must equal 1 for current parser." },
        "exportedAt": { "type": "Long", "required": true, "since": 1, "description": "Unix epoch millis when the export was created." },
        "collectionName": { "type": "String", "required": true, "since": 1, "description": "Collection display name, capped at 80 chars on import." },
        "items": { "type": "List<CollectionExportItem>", "required": true, "since": 1, "description": "Wallpapers in the collection." }
      },
      "itemFields": {
        "wallpaperId": { "type": "String", "required": true, "since": 1, "description": "Provider wallpaper ID. Sanitized on import." },
        "source": { "type": "String", "required": true, "since": 1, "description": "ContentSource enum name." },
        "thumbnailUrl": { "type": "String", "required": true, "since": 1, "description": "HTTPS thumbnail URL." },
        "fullUrl": { "type": "String", "required": true, "since": 1, "description": "HTTPS full-resolution URL." },
        "width": { "type": "Int", "required": true, "since": 1, "description": "Image width in pixels." },
        "height": { "type": "Int", "required": true, "since": 1, "description": "Image height in pixels." }
      },
      "importValidation": [
        "wallpaperId must be non-blank after trim",
        "source must match a known ContentSource enum value",
        "fullUrl must be non-blank HTTPS",
        "thumbnailUrl defaults to fullUrl if blank",
        "An item count or document size above the shared contract limit refuses the whole import before any database write",
        "Duplicate wallpaperId+source pairs are counted as skipped; invalid entries are counted as failed"
      ],
      "versionPolicy": {
        "bumpRule": "Increment version when adding required fields or changing field semantics. Optional nullable fields may be added without a version bump.",
        "forwardCompat": "Unknown fields are silently ignored by Moshi. Future versions with higher version numbers are rejected.",
        "backwardCompat": "Version 1 files remain importable. New optional fields default to null on older exports.",
        "unsupportedVersionCopy": "This collection was exported by a newer version of Aura."
      },
      "privacyExclusions": [
        "Export does not include collection creator identity or local IDs",
        "Shared collections via Firebase include creatorUid for ownership rules"
      ]
    },
    "library": {
      "currentVersion": 3,
      "sourceFile": "app/src/main/java/com/chloemlla/aura/service/LibraryExporter.kt",
      "rootClass": "LibraryExportFile",
      "mimeType": "application/json",
      "fileExtension": ".json",
      "limitKeys": [
        "MAX_FAVORITES",
        "MAX_COLLECTIONS",
        "MAX_COLLECTION_ITEMS",
        "MAX_SEARCH_HISTORY_ITEMS",
        "MAX_ROTATION_EXCLUSIONS",
        "MAX_LOCAL_WALLPAPERS",
        "MAX_WALLPAPER_HISTORY_ITEMS",
        "MAX_LIBRARY_DOCUMENT_CHARS",
        "MAX_TEXT_CHARS",
        "MAX_URL_CHARS",
        "MAX_COLLECTION_NAME_CHARS"
      ],
      "overflowPolicy": "Refuse the whole export or import before opening the destination or writing any restored row.",
      "publicationPolicy": "Serialize and sync a private staging file before opening an external destination; clean the destination and staging file after a failed or cancelled copy.",
      "portablePreferenceSections": {
        "fitCanvasPreferences": "Optional static and video Fill or Fit defaults, canvas mode, and opaque custom color. Media files and generated canvas images are not included."
      },
      "portableDataSections": {
        "rotationExclusions": "Optional persistent wallpaper and video rotation exclusions. Provider IDs, local content hashes, and one-way locator fingerprints preserve choices without exporting device paths.",
        "favoritesAndCollections": "Local entries use one portable identity across favorites and collection membership. Their original locators are blank after restore until Relink succeeds.",
        "localWallpapers": "Locator-free identity, dimensions, content hash, tags, and target for local wallpapers. Restored entries remain visible and ask the user to relink a file or folder.",
        "wallpaperHistory": "Recent wallpaper history. Device-local entries keep portable identity and metadata while their private locators are omitted."
      },
      "resultCounts": {
        "exportedOrImported": "Rows and non-empty preference sections actually written",
        "skipped": "Duplicate or migrated rows intentionally not written",
        "failed": "Invalid or device-only rows that could not be restored"
      },
      "privacyExclusions": [
        "Library backups do not include provider API keys or user credentials",
        "Library backups do not include device-local downloaded media bytes",
        "Local media locators are replaced with portable IDs before export",
        "Library backups do not include raw paths or URLs from rotation exclusions",
        "Legacy device-local download rows are reported but not restored"
      ],
      "versionPolicy": {
        "current": "Version 3 adds locator-free local wallpaper metadata and recent wallpaper history.",
        "backwardCompat": "Versions 1 and 2 remain importable. Version 1 download rows are reported as device-only instead of being silently discarded.",
        "forwardCompat": "A version newer than 3 is rejected before any database or preference write."
      }
    }
  }
}
