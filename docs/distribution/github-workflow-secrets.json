{
  "schemaVersion": 1,
  "policyKind": "githubWorkflowSecretPolicy",
  "workflowDirectory": ".github/workflows",
  "requiredWorkflowPaths": [
    ".github/workflows/aura-android.yml"
  ],
  "forbiddenTokenPatterns": [
    "github.token"
  ],
  "allowedSecrets": [
    {
      "secret": "GITHUB_TOKEN",
      "workflow": ".github/workflows/aura-android.yml",
      "allowedEnvNames": [
        "GITHUB_TOKEN",
        "token"
      ],
      "purpose": "Automatic Actions token: exported as GITHUB_TOKEN beside the lumen-crash read token for SDK resolution, and handed to the release action as its token so the run-suffixed tag and Release can be created. Read for every other job."
    },
    {
      "secret": "LUMEN_CRASH_READ_PACKAGES_TOKEN",
      "workflow": ".github/workflows/aura-android.yml",
      "allowedEnvNames": [
        "GITHUB_TOKEN",
        "LUMEN_CRASH_READ_PACKAGES_TOKEN"
      ],
      "purpose": "Read-only GitHub Packages token used to resolve the lumen-crash SDK before Gradle runs."
    },
    {
      "secret": "KEYSTORE_BASE64",
      "workflow": ".github/workflows/aura-android.yml",
      "allowedEnvNames": [
        "KEYSTORE_BASE64"
      ],
      "purpose": "Base64-encoded release signing keystore, decoded only while preparing release signing."
    },
    {
      "secret": "KEYSTORE_PASSWORD",
      "workflow": ".github/workflows/aura-android.yml",
      "allowedEnvNames": [
        "KEYSTORE_PASSWORD"
      ],
      "purpose": "Release keystore password, written only into local.properties for signing."
    },
    {
      "secret": "KEY_ALIAS",
      "workflow": ".github/workflows/aura-android.yml",
      "allowedEnvNames": [
        "KEY_ALIAS"
      ],
      "purpose": "Release signing key alias, written only into local.properties for signing."
    },
    {
      "secret": "KEY_PASSWORD",
      "workflow": ".github/workflows/aura-android.yml",
      "allowedEnvNames": [
        "KEY_PASSWORD"
      ],
      "purpose": "Release signing key password, written only into local.properties for signing."
    }
  ]
}
